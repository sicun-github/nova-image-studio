services:
  zyt-image-studio:
    image: zyt-image-studio:latest
    container_name: zyt-image-studio
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/backend/data
      - ./blacklist.json:/app/backend/blacklist.json
      - ./prompts.json:/app/backend/prompts.json
      - ./.env:/app/backend/.env
    restart: unless-stopped
